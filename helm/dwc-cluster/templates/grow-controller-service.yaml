apiVersion: v1
kind: Service
metadata:
  {{- if not .Values.growcontroller.nameOverride }}
  name: {{ include "dwc-cluster.fullname" . }}-growcontroller
  {{- else }}
  name: {{ include "dwc-cluster.fullname" . }}-{{ .Values.growcontroller.nameOverride }}
  {{- end }}
  labels:
    {{- include "dwc-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.growcontroller.service.type }}
  ports:
    - port: {{ .Values.growcontroller.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dwc-cluster.selectorLabels" . | nindent 4 }}
